server:
  port: 8081
spring:
  cloud:     config:
      server:
        git:
          default-label: circuit-breaker
          uri: https://github.com/EmanuelCabrera/microservice-spring.git
          search-paths: config-data
  application:
    name: config-service